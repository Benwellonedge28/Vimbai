fastapi==0.110.0
uvicorn==0.28.0
pydantic==2.6.4
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.9 # For file uploads
# pillow # For image processing (if local OCR is simulated)
# pytesseract # If integrating Tesseract locally (requires Tesseract-OCR installed on OS)
